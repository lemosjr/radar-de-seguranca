document.addEventListener('DOMContentLoaded', () => {

    // Nota: O módulo "header.js" já gerencia toda a lógica centralizada (Princípio DRY) do:
    // 1. Menu Hambúrguer (Sidebar)
    // 2. Verificação de Autenticação / Logout
    // 3. Atualização dos Dados de Perfil do Cabeçalho
    
    // Insira abaixo apenas funcionalidades que sejam exclusivas e únicas da página "Sobre".
});